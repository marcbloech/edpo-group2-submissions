# Local mode — all defaults apply (selfManaged Zeebe, no auth).
# No variables needed; this file exists so --env-file has something to point at.
CAMUNDA_CLIENT_MODE=selfManaged

# Market Scanner — set MARKET_MODE=REAL and supply your own Finnhub key for live data.
FINNHUB_API_KEY=<your-finnhub-api-key>
